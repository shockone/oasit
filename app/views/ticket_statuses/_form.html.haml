= form_for @ticket_status do |f|
  - if @ticket_status.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ticket_status.errors.count, "error")} prohibited this ticket_status from being saved:"
      %ul
        - @ticket_status.errors.full_messages.each do |msg|
          %li= msg
  = f.text_field :name, autofocus: ''
  .actions
    = f.submit 'Save'
